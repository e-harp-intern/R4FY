namespace :remind_notify_mailer do
  task send: :environment do
    #    users = User.all
    #    users.each do |user|
    #      RemindNotifyMailer.creation_email(user).deliver_now
    #      end
  end
end
